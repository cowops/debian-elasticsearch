---
- name: ensure apt handles https
  apt: pkg=apt-transport-https state=latest update_cache=yes force=yes

- name: install elasticsearch gpg key
  apt_key: url=https://packages.elastic.co/GPG-KEY-elasticsearch state=present

- name: add elasticsearch deb repository
  apt_repository: repo='deb https://packages.elastic.co/elasticsearch/{{ elasticsearch_version }}/debian stable main' state=present

- name: install elasticsearch packages
  apt: pkg={{ item }} state=latest update_cache=yes force=yes
  with_items:
      - elasticsearch
  notify: restart elasticsearch
